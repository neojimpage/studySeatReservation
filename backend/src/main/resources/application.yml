spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_seat_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    user:
      name: user
      password: password

server:
  port: 8080

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.studyseat.entity
  configuration:
    map-underscore-to-camel-case: true

ai-service:
  url: http://localhost:8000